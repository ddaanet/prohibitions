## Open decisions

- Which queued brief to take up next, if any — my human partner's call per the repo's own rule: `brief-deny-git-add-all.md` (new eighth hook), `brief-volatile-state-abbreviated-shas.md` (widen the sha matcher), or the one-line `check-excludedCommands.md` note (a SessionStart check, not a PreToolUse hook — may not belong in this plugin). All three sit untracked at repo root.

## Current task

`plans/2026-08-27-hook-audit-fixes.md` is fully executed: all six tasks landed and `just precommit` is green. What remains is this repo's release sequence — `ddaa:preflight`, then `/gitlore:push`, then `just release`.

## Current task

Nothing in flight. The `ddaanet` tier's index/description reconciliation is finished and verified: 13 descriptions synced from their canonical index lines, two lines amended to recover the `local_agent` and `CLAUDE.md` triggers, and root, carrier and descriptions now agree across all 85 entries.

The session's other thread, the exact-`---` frontmatter fence in `deny-volatile-memory-state.sh`, is settled and pinned by tests in both directions.
